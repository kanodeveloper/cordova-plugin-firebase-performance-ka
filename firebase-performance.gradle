buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.google.firebase:firebase-plugins:1.+'
    }
}

repositories {
    maven {
        url 'https://maven.google.com/'
    }
}

apply plugin: com.google.firebase.perf.FirebasePerformance
//com.google.firebase.firebase-perf
